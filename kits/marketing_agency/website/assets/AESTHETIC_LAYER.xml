<?xml version="1.0" encoding="UTF-8"?>
<aesthetic_layer version="1.0">
    <description>
        Reusable UI/UX aesthetic tokens and CSS presets for high-end web projects.
    </description>

    <tokens>
        <token name="highlight-color">#3b82f6</token> <!-- Brand Blue -->
        <token name="scrollbar-width">8px</token>
        <token name="scrollbar-track">rgba(0,0,0,0.05)</token>
        <token name="scrollbar-thumb">rgba(0,0,0,0.2)</token>
        <token name="animation-spring">cubic-bezier(0.175, 0.885, 0.32, 1.275)</token>
        <token name="animation-smooth">cubic-bezier(0.4, 0, 0.2, 1)</token>
        <token name="void-bg">#050505</token>
        <token name="focus-glow-blue">rgba(59, 130, 246, 0.5)</token>
        
        <!-- Trust Palette -->
        <token name="trust-primary">#0F172A</token>
        <token name="trust-cta">#0369A1</token>
        
        <!-- Luxury Palette -->
        <token name="luxury-primary">#1C1917</token>
        <token name="luxury-cta">#CA8A04</token>
        
        <!-- Tech Palette -->
        <token name="tech-primary">#6366F1</token>
        <token name="tech-cta">#10B981</token>
    </tokens>

    <css_presets>
        <preset name="pro-max-neumorphism"><![CDATA[
/* Neumorphism 2.0 (Tactile) */
.neumorphic {
  background: var(--background, #f0f0f0);
  box-shadow: 8px 8px 16px #d1d1d1, -8px -8px 16px #ffffff;
  border-radius: 16px;
}
        ]]></preset>

        <preset name="pro-max-linear"><![CDATA[
/* Linear/Vercel Aesthetic */
.linear-card {
  background: #1A1A1A;
  border: 1px solid #333333;
  color: #FFFFFF;
}
        ]]></preset>
        <preset name="custom-scrollbar"><![CDATA[
/* Custom Dark Scroller */
::-webkit-scrollbar {
  width: var(--scrollbar-width, 8px);
}
::-webkit-scrollbar-track {
  background: var(--void-bg, #050505);
}
::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.1);
  border-radius: 10px;
}
::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.2);
}
        ]]></preset>

        <preset name="selection-highlight"><![CDATA[
/* Custom Highlight Color */
::selection {
  background: var(--highlight-color, #3b82f6);
  color: #fff;
}
        ]]></preset>

        <preset name="hover-lift"><![CDATA[
/* Hover Animation: Lift & Spring */
.hover-lift {
  transition: transform 0.3s var(--animation-spring, cubic-bezier(0.175, 0.885, 0.32, 1.275));
}
.hover-lift:hover {
  transform: translateY(-4px);
}
        ]]></preset>

        <preset name="fade-in-stagger"><![CDATA[
/* Staggered Fade-in Entry */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}
.stagger-item {
  animation: fadeIn 0.5s var(--animation-smooth, ease-out) forwards;
  opacity: 0;
}
        ]]></preset>

        <preset name="glassmorphism-v2"><![CDATA[
/* High-Fidelity Glass v2 */
.glass-v2 {
  backdrop-filter: blur(10px) saturate(180%);
  background: rgba(255, 255, 255, 0.03);
  border: 1px solid rgba(255, 255, 255, 0.08);
}
        ]]></preset>

        <preset name="linear-border-beam"><![CDATA[
/* Animated Gradient Border Beam */
@keyframes beam {
  0% { background-position: 0% 50%; }
  100% { background-position: 200% 50%; }
}
.border-beam {
  position: relative;
  background: linear-gradient(90deg, transparent, var(--highlight-color, #3b82f6), transparent);
  background-size: 200% 100%;
  animation: beam 2s infinite linear;
}
        ]]></preset>

        <preset name="aurora-mesh"><![CDATA[
/* Organic Aurora Mesh Gradient */
.aurora-mesh {
  background: radial-gradient(at 0% 0%, rgba(59, 130, 246, 0.15) 0px, transparent 50%),
              radial-gradient(at 100% 0%, rgba(16, 185, 129, 0.1) 0px, transparent 50%),
              radial-gradient(at 100% 100%, rgba(99, 102, 241, 0.15) 0px, transparent 50%),
              radial-gradient(at 0% 100%, rgba(236, 72, 153, 0.1) 0px, transparent 50%);
}
        ]]></preset>
    </css_presets>
</aesthetic_layer>
